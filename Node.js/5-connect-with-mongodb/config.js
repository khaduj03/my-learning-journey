// We define the port here to use it in the main app file
module.exports = {
  port: 3004,

  // This is used for error handling.
  // If `debug` is true, show detailed error messages (for development).
  // If `debug` is false, show user-friendly error messages (for production).
  debug: false,
};
